extension UILocalizedIndexedCollation {

  @discardableResult
  func sectionForSectionIndexTitleAtIndex(sectionForSectionIndexTitleAtIndex indexTitleIndex: NSInteger) -> Self {
return self
}

  @discardableResult
  func sectionForObject(sectionForObject object: id, collationStringSelector selector: SEL) -> Self {
return self
}

  @discardableResult
  func sortedArrayFromArray(sortedArrayFromArray array: NSArray, collationStringSelector selector: SEL) -> Self {
return self
}
}
