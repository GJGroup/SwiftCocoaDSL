extension UIPreviewInteraction {

  @discardableResult
  func initWithView(initWithView view: UIView) -> Self {
return self
}
    @discardableResult
  func view(_ view: UIView) -> Self {
self.view = view
return self
}

  @discardableResult
  func ()->Self {
return self
}

  @discardableResult
  func locationInCoordinateSpace(locationInCoordinateSpace cancelInteraction: void) -> Self {
return self
}

  @discardableResult
  func previewInteraction(previewInteraction previewInteraction: UIPreviewInteraction, didUpdatePreviewTransition transitionProgress: CGFloat, ended ended: BOOL, UIKIT_AVAILABLE_IOS_ONLY previewInteractionDidCancel: void, UIKIT_AVAILABLE_IOS_ONLY previewInteraction: UIPreviewInteraction, UIKIT_AVAILABLE_IOS_ONLY previewInteractionShouldBegin: BOOL, UIKIT_AVAILABLE_IOS_ONLY previewInteraction: UIPreviewInteraction, UIKIT_AVAILABLE_IOS_ONLY previewInteraction: void, UIKIT_AVAILABLE_IOS_ONLY previewInteraction: UIPreviewInteraction, didUpdateCommitTransition transitionProgress: CGFloat, ended ended: BOOL) -> Self {
return self
}
}
