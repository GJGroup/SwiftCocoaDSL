extension UIScreen {
    @discardableResult
  func mainScreen(_ mainScreen: UIScreen) -> Self {
self.mainScreen = mainScreen
return self
}
    @discardableResult
  func bounds(_ bounds: CGRect) -> Self {
self.bounds = bounds
return self
}
    @discardableResult
  func scale(_ scale: CGFloat) -> Self {
self.scale = scale
return self
}
    @discardableResult
  func preferredMode(_ preferredMode: UIScreenMode) -> Self {
self.preferredMode = preferredMode
return self
}
    @discardableResult
  func currentMode(_ currentMode: UIScreenMode) -> Self {
self.currentMode = currentMode
return self
}
    @discardableResult
  func currentMode(_ currentMode: UIScreenMode) -> Self {
self.currentMode = currentMode
return self
}
    @discardableResult
  func overscanCompensation(_ overscanCompensation: UIScreenOverscanCompensation) -> Self {
self.overscanCompensation = overscanCompensation
return self
}
    @discardableResult
  func overscanCompensationInsets(_ overscanCompensationInsets: UIEdgeInsets) -> Self {
self.overscanCompensationInsets = overscanCompensationInsets
return self
}
    @discardableResult
  func mirroredScreen(_ mirroredScreen: UIScreen) -> Self {
self.mirroredScreen = mirroredScreen
return self
}
    @discardableResult
  func brightness(_ brightness: CGFloat) -> Self {
self.brightness = brightness
return self
}
    @discardableResult
  func wantsSoftwareDimming(_ wantsSoftwareDimming: BOOL) -> Self {
self.wantsSoftwareDimming = wantsSoftwareDimming
return self
}
    @discardableResult
  func nativeBounds(_ nativeBounds: CGRect) -> Self {
self.nativeBounds = nativeBounds
return self
}
    @discardableResult
  func nativeScale(_ nativeScale: CGFloat) -> Self {
self.nativeScale = nativeScale
return self
}

  @discardableResult
  func displayLinkWithTarget(displayLinkWithTarget target: id, selector sel: SEL, NS_AVAILABLE_IOS NSInteger: readonly, maximumFramesPerSecond id: readonly, nullable UIView: readonly, focusedView BOOL: nonatomic, supportsFocus CGRect: readonly, applicationFrame UIView: UISnapshotting, applicationFrame snapshotViewAfterScreenUpdates: UISnapshotting, applicationFrame afterUpdates: BOOL) -> Self {
return self
}
}
